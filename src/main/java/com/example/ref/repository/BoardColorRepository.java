package com.example.ref.repository;

import com.example.ref.entity.BoardColor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoardColorRepository extends JpaRepository<BoardColor, Long> {

}
